0 Script that prints "Hello World"
1 Script that displays confused smiley "√√(√îo)'
2 Script that display contents of /etc/passwd
3 Script that display contents of /etc/passwd and /etc/hosts
4 Script that display last 10 lines of /etc/passwd
5 Script that display first 10 lines of /etc/passwd
6 Script that display third line of the file iacta
7 Script that creates a file named \*\\'"Best School"\'\\*$\?\*\*\*\*\*:) containing text "Best School"
8 Script that writes current state of directory to ls_cwd_content
9 Script that duplicates the last line of the file iacta
10 Script that deletes files with .js extension in current directory and sub directories
11 Script that counts directories and sub directories in current directory
12 Script that displays 10 newest files in current directory
13 Script that removes duplicates
14 Script that displays lines containing "root" in /etc/passwd
15 Script that displays the number of lines that contain the pattern bin in /etc/passwd
16 Script that displays the lines containing the pattern "root" and 3 lines after them
17 Script that displays all the lines in /etc/passwd that don't contain "bin"
18 Script that displays all lines of the file /etc/ssh/sshd_config starting with a letter
19 Script that replace charcters 'A' and 'c' from input with 'Z' and 'e' respectively
20 Script that removes letters 'c' and 'C' from input
21 Script that reverses its input
22 Script that lists all users and their home directories sorted by users
